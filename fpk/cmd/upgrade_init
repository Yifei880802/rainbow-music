#!/bin/bash
#
# Rainbow 音乐下载 — 升级钩子：upgrade_init
#
# 升级文件应用前执行：保数据钩子。
# 数据目录（${TRIM_PKGVAR}）与配置（${TRIM_PKGETC}）均不在升级覆盖范围内，
# 天然保数据，无需额外动作。

set -eu

# shellcheck source=/dev/null
source "$(cd "$(dirname "$0")" && pwd)/_common"

# 无需额外动作：${TRIM_PKGVAR}（数据）与 ${TRIM_PKGETC}（配置）不在升级覆盖范围内
exit 0
