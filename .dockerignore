# 构建上下文瘦身：只把源码 + web + 依赖清单带进去
server/node_modules
server/dist
server/test
**/*.log
# v0.2.9：data 下仅放行 sources（镜像内置音源副本 COPY data/sources 需要），
# downloads/db/临时库/日志等仍排除

data/*
!data/sources
config.yaml
.git
scripts
lx-music-desktop
**/.DS_Store
